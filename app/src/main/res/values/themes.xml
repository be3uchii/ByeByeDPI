<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Используем встроенную тему устройства. Легкая и быстрая. -->
    <style name="Theme.ByeDPI" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <!-- Цвета берутся системные, переопределять не обязательно, но можно -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
